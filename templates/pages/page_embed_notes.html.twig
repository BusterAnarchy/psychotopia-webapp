{% extends "base_embed.html.twig" %}

{% block title %}Notes - {{ molecule.getLabel() }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .embed-notes {
            --notes-ink: #1b1b1b;
            --notes-muted: #576664;
            --notes-bg: #fffaf3;
            --notes-card: #ffffff;
            --notes-accent: #1d7874;
            --notes-accent-strong: #0f4c48;
            --notes-border: rgba(29, 120, 116, 0.25);
            --notes-shadow: 0 20px 40px rgba(11, 41, 39, 0.16);
            width: 100%;
            min-height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            background:
                radial-gradient(900px 360px at -10% -20%, rgba(29, 120, 116, 0.16), transparent 60%),
                radial-gradient(520px 260px at 110% -10%, rgba(248, 198, 99, 0.2), transparent 65%),
                var(--notes-bg);
        }

        .embed-notes__card {
            position: relative;
            width: 100%;
            max-width: 760px;
            background: var(--notes-card);
            border: 1px solid var(--notes-border);
            border-radius: 16px;
            padding: 18px 22px;
            box-shadow: var(--notes-shadow);
            overflow: hidden;
            animation: note-rise 420ms ease-out;
        }

        .embed-notes__card::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 6px;
            height: 100%;
            background: linear-gradient(180deg, var(--notes-accent), rgba(29, 120, 116, 0.3));
        }

        .embed-notes__eyebrow {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 8px;
        }

        .embed-notes__pill {
            background: var(--notes-accent-strong);
            color: #ffffff;
            padding: 4px 12px;
            border-radius: 999px;
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            font-weight: 700;
        }

        .embed-notes__brand {
            font-size: 0.8rem;
            font-weight: 600;
            letter-spacing: 0.05em;
            text-transform: uppercase;
            color: var(--notes-muted);
        }

        .embed-notes__title {
            font-size: 1.65rem;
            line-height: 1.15;
            margin: 0;
            color: var(--notes-ink);
        }

        .embed-notes__title-row {
            display: flex;
            align-items: baseline;
            gap: 10px; /* space between title and form */
            margin-bottom: 2px;
        }

        .embed-notes__form {
            align-self: center;
            position: static;
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            font-weight: 700;
            color: var(--notes-accent-strong);
            background: rgba(29, 120, 116, 0.12);
            border: 1px solid rgba(29, 120, 116, 0.3);
            padding: 3px 10px;
            border-radius: 999px;
        }

        .embed-notes__subtitle {
            font-size: 0.88rem;
            color: var(--notes-muted);
        }

        .embed-notes__stats {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 10px;
            margin-top: 12px;
        }

        .embed-notes__stat {
            background: #f3f7f6;
            border: 1px solid #e0e9e8;
            border-radius: 12px;
            padding: 10px 12px;
        }

        .embed-notes__stat-label {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            color: var(--notes-muted);
            margin-bottom: 6px;
            font-weight: 600;
        }

        .embed-notes__stat-value {
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--notes-ink);
            display: flex;
            align-items: baseline;
            gap: 6px;
        }

        .embed-notes__unit {
            font-size: 0.85rem;
            font-weight: 600;
            color: var(--notes-muted);
        }

        .embed-notes__summary {
            margin-top: 16px;
            font-size: 0.98rem;
            color: var(--notes-muted);
        }

        .embed-notes__context {
            margin-top: 10px;
            padding: 8px 12px;
            border-radius: 10px;
            border: 1px dashed rgba(29, 120, 116, 0.2);
            background: rgba(29, 120, 116, 0.08);
            color: #2d3b39;
            font-size: 0.86rem;
            line-height: 1.35;
        }

        .embed-notes__context strong {
            color: var(--notes-accent-strong);
            font-weight: 700;
        }

        .embed-notes__note {
            margin-top: 16px;
            padding: 12px 14px;
            background: #eef5f4;
            border-left: 3px solid var(--notes-accent);
            border-radius: 10px;
            color: #2d3b39;
            font-size: 0.92rem;
        }

        .embed-notes__footer {
            margin-top: 12px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
            flex-wrap: wrap;
        }

        .embed-notes__link {
            color: var(--notes-accent-strong);
            font-weight: 700;
            background: rgba(29, 120, 116, 0.12);
            border: 1px solid rgba(29, 120, 116, 0.3);
            border-radius: 999px;
            padding: 8px 14px;
            transition: transform 160ms ease, box-shadow 160ms ease, background 160ms ease;
        }

        .embed-notes__link:hover {
            transform: translateY(-1px);
            box-shadow: 0 8px 16px rgba(29, 120, 116, 0.15);
            background: rgba(29, 120, 116, 0.18);
        }

        .embed-notes__source {
            font-size: 0.8rem;
            color: var(--notes-muted);
            text-transform: uppercase;
            letter-spacing: 0.05em;
            font-weight: 600;
        }

        .tooltip {
            display: inline-block;
            margin-left: 6px;
            width: 16px;
            height: 16px;
            line-height: 16px;
            text-align: center;
            border-radius: 50%;
            background: #ccc;
            color: #1b1b1b;
            font-size: 12px;
            cursor: help;
            position: relative;
        }

        .tooltip-text {
            text-transform: none;
            visibility: hidden;
            opacity: 0;
            position: absolute;
            bottom: 125%;
            left: 470%;
            transform: translateX(-50%);
            margin-top: 100px;
            padding: 8px 12px;
            border-radius: 10px;
            border: 1px dashed rgb(200, 218, 218);
            background: rgb(238, 244, 244);
            color: #2d3b39;
            font-size: 0.86rem;
            line-height: 1.35;
            font-weight: 400;
            white-space: normal;
            width: 350px;
            transition: opacity 0.2s;
            z-index: 10;
        }

        .tooltip:hover .tooltip-text {
            visibility: visible;
            opacity: 1;
        }

        @keyframes note-rise {
            from {
                opacity: 0;
                transform: translateY(12px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 520px) {
            .embed-notes {
                padding: 14px;
            }

            .embed-notes__card {
                padding: 16px;
            }

            .embed-notes__title {
                font-size: 1.4rem;
            }

            .embed-notes__stat-value {
                font-size: 1.3rem;
            }
        }

        @media (max-width: 720px) {
            .embed-notes__stats {
                grid-template-columns: repeat(2, minmax(0, 1fr));
            }
        }

        @media (max-width: 480px) {
            .embed-notes__stats {
                grid-template-columns: 1fr;
            }
        }

        @media (prefers-reduced-motion: reduce) {
            .embed-notes__card {
                animation: none;
            }

            .embed-notes__link {
                transition: none;
            }
        }
    </style>
{% endblock %}

{% block body %}

{% set is_weight_unit = unit == 'poids' %}
{% set analysis_route = is_weight_unit ? 'app_purity_tablets' : (molecule.hasContentAnalysis ? 'app_content' : 'app_purity') %}
{% set analysis_label = is_weight_unit ? 'comprimés' : (molecule.hasContentAnalysis ? 'teneur' : 'pureté') %}
{% set unit_label = unit == 'pourcent' ? '%' : (is_weight_unit ? 'g' : unit) %}
{% set average_label = is_weight_unit ? 'Poids moyen' : analysis_label ~ ' moyenne' %}
{% set average_precision = is_weight_unit ? 2 : 1 %}

<section class="embed-notes">
    <article class="embed-notes__card" aria-label="Notes de l'observatoire">
        <header class="embed-notes__header">
            <div class="embed-notes__eyebrow">
                <span class="embed-notes__pill">Notes de l'observatoire</span>
                <span class="embed-notes__brand">Psychotopia</span>
            </div>
            <div class="embed-notes__title-row">
                <h1 class="embed-notes__title">{{ molecule.getLabel() }}</h1>
                {% if form_label is defined and form_label %}
                    <span class="embed-notes__form">{{ form_label }}</span>
                {% endif %}
            </div>
            <p class="embed-notes__subtitle">Synthèse rapide concernant cette molecule.</p>
        </header>

        <div class="embed-notes__stats">
            <div class="embed-notes__stat">
                <div class="embed-notes__stat-label">{{ average_label }}</div>
                <div class="embed-notes__stat-value">
                    {{ results.average|number_format(average_precision, '.', ' ') }}
                    <span class="embed-notes__unit">{{ unit_label }}</span>
                </div>
            </div>
            <div class="embed-notes__stat">
                <div class="embed-notes__stat-label">Facteur de variabilité
                    <span class="tooltip">?
                        <span class="tooltip-text">Le facteur de variabilité est le rapport du dernier sur le premier décile.</span>
                    </span>
                </div>
                <div class="embed-notes__stat-value">
                    {{results.variability_factor.factor|number_format(1, '.', ' ')}}
                </div>
            </div>
            <div class="embed-notes__stat">
                <div class="embed-notes__stat-label">Nb. Echantillons</div>
                <div class="embed-notes__stat-value">
                    {{results.count|number_format(0, '.', ' ')}}
                </div>
            </div>
        </div>

        {% if is_weight_unit %}
            <p class="embed-notes__context">En pratique, dans un comprimé, la quantité de psychoactif peut passer du simple au <strong>{{ results.variability_factor.factor |number_format(1, '.', ' ') }}</strong> -uple, soit de  <strong>{{ results.variability_factor.lower_bound |number_format(0, '.', ' ') }}&nbspmg</strong> a <strong>{{ results.variability_factor.upper_bound |number_format(0, '.', ' ') }}&nbspmg</strong> de quantité.</p>
        {% else %}
            <p class="embed-notes__context">En pratique, a quantité égale, l'echantillion peut passer du simple au <strong>{{ results.variability_factor.factor |number_format(1, '.', ' ') }}</strong> - uple en teneur psychoactive, soit de <strong>{{ results.variability_factor.lower_bound |number_format(0, '.', ' ') }}%</strong> a <strong>{{ results.variability_factor.upper_bound |number_format(0, '.', ' ') }}%</strong> de {{ analysis_label }}.</p>
        {% endif %}

        <footer class="embed-notes__footer">
            <a class="embed-notes__link" href="{{ path(analysis_route, { slug: molecule.getSlug() }) }}" target="_blank" rel="noopener noreferrer">
                Voir la page Psychotopia correspondante
            </a>
            <span class="embed-notes__source">Source : Psychotopia</span>
        </footer>
    </article>
</section>
{% endblock %}
